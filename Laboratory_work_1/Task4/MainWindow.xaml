<Window x:Class="Task4.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сложение чисел" 
        Height="400" Width="500"
        Background="White"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Общие стили -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#F8F8F8"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#005A9E"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="Black"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Заголовок -->
            <RowDefinition Height="Auto"/>
            <!-- Число 1 -->
            <RowDefinition Height="Auto"/>
            <!-- Число 2 -->
            <RowDefinition Height="Auto"/>
            <!-- Кнопки -->
            <RowDefinition Height="*"/>
            <!-- Результат -->
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" 
                   Text="Сложение чисел" 
                   FontSize="20" FontWeight="Bold" 
                   Foreground="#007ACC" HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- Первое число -->
        <Label Grid.Row="1" Grid.Column="0" Content="Число 1:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Name="number1TextBox" Height="35"/>

        <!-- Второе число -->
        <Label Grid.Row="2" Grid.Column="0" Content="Число 2:" Margin="0,15,0,0"/>
        <TextBox Grid.Row="2" Grid.Column="1" Name="number2TextBox" Height="35" Margin="0,15,0,0"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Name="addButton" Content="Сложить" Click="AddButton_Click"/>
            <Button Name="clearButton" Content="Очистить" Click="ClearButton_Click"/>
        </StackPanel>

        <!-- Результат -->
        <Border Grid.Row="4" Grid.ColumnSpan="2" Margin="0,25,0,0" BorderBrush="#CCCCCC" BorderThickness="1" Padding="10" Background="#F8F8F8">
            <StackPanel>
                <TextBlock Text="Результат:" FontWeight="Bold" FontSize="14"/>
                <TextBlock Name="resultTextBlock" FontSize="16" FontWeight="Bold" Foreground="#007ACC" Margin="0,5,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>